{% extends 'layout/admin_base.html.twig' %}

{% block title %}User détails{% endblock %}

{% block body %}
<div id="main" class="main-content flex-1 bg-gray-100 mt-12 md:mt-2 pb-24 md:pb-5 p-20">
    <div class="card">
        <h3 class="title">{{ user.email }}</h3>
        <div class="image-card">
            <img src="{{ asset('asset/images/logo-paul.svg') }}"/>
        </div>
        <div class="description-card">
            <p><span>Nom : </span>{{ user.name }}</p>
            <p><span>Prénom : </span>{{ user.surname }}</p>
            <div>
                <span>Rôles : </span>
                <ul>
                    {% for role in user.roles %}
                        <li>- {{ role }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% if user.isVerified == true %}
                <p><span>Status : </span>Vérifier</p>
            {% else %}
                <p><span>Status : </span>Non Vérifier</td>
            {% endif %}
            {% if user.actif %}
                <p><span>Status : </span>Actif</p>
                <div>
                    <a href="{{ path('disable-user', {id: user.id}) }}" class="action">Rendre inactif</a>
            {% else %}
                <p><span>Status : </span>Inactif</p>
                <div>
                    <a href="{{ path('active-user', {id: user.id}) }}" class="action">Rendre actif</a>
            {% endif %}
                <a href="{{ path('active-annonce', {id: user.id}) }}" class="action">Envoyer message d'avertissement</a>
            </div>
        </div>
    </div>
    <div class="m-t-20">
        <a href="{{ path('all-users') }}" class="action width-100 text-align-center inline-block">Retour listing Users</a>
    <div>
</div>
{% endblock %}
