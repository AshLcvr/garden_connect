{% extends 'layout/admin_base.html.twig' %}

{% block title %}Modifier catégorie{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('Formulaires') }}
{% endblock %}

{% block body %}
<div id="main" class="main-content flex-1 bg-gray-100 mt-12 md:mt-2 pb-24 md:pb-5 p-20">
    <h2 class="title">Modifier catégorie</h2>
    <div class="m-b-20">
        {{ include('admin/category/_delete_form.html.twig') }}
    </div>
    {{ include('admin/category/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="m-t-20">
        <a href="{{ path('app_category_show', {id: category.id}) }}" class="btn btn-primary">Retour aux détails</a>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('Formulaires') }}
{% endblock %}