<form class="m-t-20" method="post" action="{{ path('delete-sous-cat', {'id': subcategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subcategory.id) }}">
    <button class="action text-align-center inline-block">Supprimer</button>
</form>
