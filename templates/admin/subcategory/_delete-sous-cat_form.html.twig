<form method="post" action="{{ path('delete-sous-cat', {'id': subcategory.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette sous-catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subcategory.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
